/**
 * Shared UI Components (Design System)
 *
 * Barrel export file — cho phép import gọn gàng từ một nơi duy nhất.
 * Ví dụ: import { Button } from '@/shared/ui'
 *
 * Khi thêm component shadcn/ui mới (vd: npx shadcn add input),
 * nhớ re-export ở đây.
 */
export { Button, buttonVariants } from './button'
